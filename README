1. Just use these files to replace the corresponding files in the kernel (`pathtokernel/drivers/md`).
2. `cd pathtokernel/drivers/md` and `make`.
3. Install the output modules `md-mod.o` and `raid456.o`.